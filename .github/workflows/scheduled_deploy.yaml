name: Scheduled Update and Deploy

on:
  schedule:
    - cron: "0 16 * * *"
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Set up SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_SECRET }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan github.com >> ~/.ssh/known_hosts

      - name: Config Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
      
      - name: Update config for author
        run: |
          if $(ssh -T git@github.com 2>&1 | grep -q 'Hi LittleNyima!'); then
            echo 'username: LittleNyima' > config.yaml
            echo 'title: 境界なきキネマ録' >> config.yaml
            echo 'slogan: カオスから芸術まで、物語の深淵を覗き込む。' >> config.yaml
          fi

      - name: Build and Deploy
        run: bun run deploy
        env:
          NODE_ENV: production
